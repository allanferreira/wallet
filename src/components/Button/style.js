import styled from 'styled-components'
import { colors } from '@/services/styledVariables'

export const Button = styled.div`
    padding: 10px;
    margin-bottom: 10px;
    border: 1px solid ${colors.support.bg};
    color: ${colors.support.text};
    cursor: pointer;
    text-align: center;
`
